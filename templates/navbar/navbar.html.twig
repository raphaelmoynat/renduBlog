<nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
        {% if app.user %}
            {% if app.user.image is not empty %}
                {% for image in app.user.image %}
            <a class="navbar-brand active" aria-current="page" href="{{ path("user_avatar",{id:app.user.id}) }}">
                <img class="img-fluid mx-1  rounded-3" style="max-width: 150px; max-height: 150px;" src="{{ vich_uploader_asset(image)|imagine_filter('petite_vignette') }}" alt="">
            </a>
                {% endfor %}
            {% else %}
                <a class="navbar-brand active" aria-current="page" href="{{ path("user_avatar",{id:app.user.id}) }}" >
                    <img class="img-fluid mx-1  rounded-3" style="max-width: 150px; max-height: 150px;" src="{{ asset("images/no_image_profile.png")|imagine_filter('petite_vignette') }}" alt="">
                </a>

            {% endif %}

        {% else %}
            <a class="navbar-brand" href="#">Blog</a>


        {% endif %}

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="{{ path("app_article") }}">Articles</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("app_create_article") }}">Créer</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("app_category") }}">Catégories</a>
                </li>
                {% if app.user %}
                    <a class="nav-link" href="{{ path('app_logout') }}">Log out</a>
                {% else %}
                    <a class="nav-link" href="{{ path('app_register') }}">Register</a>
                    <a class="nav-link" href="{{ path('app_login') }}">Log in</a>
                {% endif %}




            </ul>
        </div>
    </div>
</nav>